zk:
  homepage:products_list.zul

spring:
  application:
    name:zk-demo
  datasource:
    url: jdbc:postgresql://localhost:5432/zk_with_pringboot_users
    username: postgres
    password: Madede_11
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect